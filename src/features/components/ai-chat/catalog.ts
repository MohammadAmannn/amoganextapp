// Add these to your componentTypes object
/*
Heading: {
  props: z.object({
    level: z.enum(['1', '2', '3', '4', '5', '6']).default('2'),
    children: z.string().optional(),
  }),
},
Text: {
  props: z.object({
    children: z.string().optional(),
    size: z.enum(['sm', 'base', 'lg', 'xl']).default('base'),
  }),
},
Price: {
  props: z.object({
    amount: z.string(),
    period: z.string().optional(),
  }),
},
FeatureList: {
  props: z.object({
    features: z.array(z.string()).default([]),
  }),
},
*/